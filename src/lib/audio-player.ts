/** Re-exports player store for backward compatibility. */
export {
  stop,
  toggle,
  toggleTrack,
  usePlayback,
  usePlayer,
  playTrack,
  playNext,
  playPrev,
  togglePlayPause,
  setQueue,
} from "./player-store";
